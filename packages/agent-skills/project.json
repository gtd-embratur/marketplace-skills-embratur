{
  "name": "@embratur/agent-skills",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/agent-skills/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{options.outputPath}"],
      "options": {
        "command": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js --format=esm",
        "cwd": "packages/agent-skills"
      }
    }
  }
}
